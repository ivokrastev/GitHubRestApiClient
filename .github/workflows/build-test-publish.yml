name: Build and Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

env:
  AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}

permissions:
  actions: read
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
        packages: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 8.x
      env:
        NUGET_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
        
    - name: Restore dependencies
      run: dotnet restore
      
    - name: Build
      run: dotnet build -c Release --property GenerateLocalNugets=true
      
    - name: Restore dependencies with latest local NuGet packages
      run: dotnet restore -s ./artifacts/nuget --property UseProjectReferences=false
      
    - name: Build using local NuGet packages
      run: dotnet build -c Release --property UseProjectReferences=false
      
    - name: Test
      run: ./TestApp/bin/Release/net8.0/TestApp ${{ github.repository_owner }} ${{ github.event.repository.name }} ${{ secrets.GITHUB_TOKEN }}
      
    - name: Setup authentication for the NuGet repository
      run: dotnet nuget add source --username ${{ github.repository_owner }} --password ${{ secrets.GITHUB_TOKEN }} --store-password-in-clear-text --name github "https://nuget.pkg.github.com/ivokrastev/index.json"
      
    - name: Push to GitHub package registry
      run: dotnet nuget push ./artifacts/nuget/*.nupkg --source "github" --api-key ${{ secrets.GITHUB_TOKEN }}
